{% extends 'base.html.twig' %}

{% block title %}Locution{% endblock %}

{% block body %}
    <h1>Locution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ locution.id }}</td>
            </tr>
            <tr>
                <th>Locution</th>
                <td>{{ locution.locution }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ locution.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('locution_index') }}">back to list</a>

    <a href="{{ path('locution_edit', {'id': locution.id}) }}">edit</a>

    {{ include('locution/_delete_form.html.twig') }}
{% endblock %}
