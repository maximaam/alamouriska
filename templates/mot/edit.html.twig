{% extends 'base.html.twig' %}

{% block title %}Edit Mot{% endblock %}

{% block body %}
    <h1>Edit Mot</h1>

    {{ include('mot/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('mot_index') }}">back to list</a>

    {{ include('mot/_delete_form.html.twig') }}
{% endblock %}
