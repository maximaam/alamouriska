<strong>
    <a href="{{ path('user_show', { 'username': user.username }) }}" title="Ajouté(e) par: {{ user.username }}">{{ user.username }}</a>
</strong>
<br>
<span class="text-secondary" title="Publié(e) le">{{ action_date | date }}</span>