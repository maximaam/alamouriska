<strong>
    <a href="{{ path('user_show', { 'username': user.username }) }}" title="Ajouté(e) par: {{ user.username }}">
        {% if is_mobile(app.request.headers) is same as(true) %}
            {{ user.username | wordwrap(18) }}
        {% else %}
            {{ user.username }}
        {% endif %}
    </a>
</strong>
<br>
<span class="text-secondary" title="Publié(e) le">{{ action_date | date }}</span>