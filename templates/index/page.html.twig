{% extends 'base.html.twig' %}

{% block title %}
    {{ page.title }}
    {{ parent() }}
{% endblock %}

{% block body %}

    <div class="container">
        <h1>{{ page.title }}</h1>
        <p class="text-justify">{{ page.description | raw | nl2br }}</p>
    </div>

{% endblock %}
