{% extends 'base.html.twig' %}

{% block title %}
    {{ page.title }}
    {{ parent() }}
{% endblock %}

{% block body %}

    <div class="container">
        <h1>{{ page.title }}</h1>
        <p class="text-justify">{{ page.description | raw }}</p>
    </div>

{% endblock %}
